export interface Language {
  ro: string;
  ru: string;
}

export interface AboutData {
  title: Language;
  description: Language;
}

export interface CategoryTextData {
  description: Language;
  metaTitle: string;
  metaDescription: string;
}
